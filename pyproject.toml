[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "igdl"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["instaloader"]

[project.scripts]
igdl = "igdl.cli:main"
